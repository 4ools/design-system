import { GridConfig } from './interfaces';

const grid: GridConfig = {
  colGap: '20px',
  rowGap: '20px',
  rows: 'auto',
  cols: 'repeat(12 1fr)',
  justify: 'start',
  align: 'start',
};

export default grid;
