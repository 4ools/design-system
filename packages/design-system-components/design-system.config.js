// this is how we configure our little design system site
module.exports = {
  // where are your components, we maybe do not need this later I think as it will be
  // in the same place the user is anyway
  repository: 'https://github.com/4ools/design-system.git',
  root: 'packages/design-system-components/',
};
